<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>MySystem Password Reset</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <style>
    body { font-family: Arial, sans-serif; background:#f4f6f9; margin:0; padding:0; -webkit-text-size-adjust:100%; }
    .container { max-width:600px; margin:20px auto; background:#fff; border-radius:8px; padding:0; box-shadow:0 2px 8px rgba(0,0,0,0.06); overflow:hidden; }
    .header { background:#2563eb; padding:24px; text-align:center; }
    .header h1 { color:#fff; font-size:24px; margin:0; }
    .content { padding:24px; }
    h2 { color:#2563eb; font-size:20px; margin:0 0 16px; }
    p { font-size:15px; color:#333; line-height:1.6; margin:10px 0; }
    .icon { text-align:center; margin:20px 0; }
    .icon-circle { display:inline-block; width:60px; height:60px; background:#eff6ff; border-radius:50%; line-height:60px; }
    .cta { display:inline-block; text-decoration:none; padding:14px 28px; border-radius:6px; background:#2563eb; color:#fff; font-weight:600; text-align:center; margin:16px 0; }
    .cta:hover { background:#1d4ed8; }
    .link-box { background:#f3f4f6; padding:12px; border-radius:6px; word-break:break-all; margin:16px 0; }
    .link-box a { color:#2563eb; font-size:14px; }
    .warning { font-size:13px; color:#6b7280; background:#f9fafb; padding:12px; border-radius:6px; margin:16px 0; border-left:3px solid #2563eb; }
    .small { font-size:13px; color:#6b7280; }
    .footer { text-align:center; font-size:13px; color:#9ca3af; padding:20px; background:#f9fafb; border-top:1px solid #e5e7eb; }
    .logo { font-weight:bold; color:#2563eb; }
    @media only screen and (max-width:480px) {
      .content { padding:16px; }
      h2 { font-size:18px; }
    }
  </style>
</head>
<body>
  <div style="display:none; max-height:0; overflow:hidden; font-size:1px; line-height:1px; color:#fff; opacity:0;">Reset your MySystem password.</div>

  <div class="container">
    <div class="header">
      <h1>MySystem</h1>
    </div>

    <div class="content">
      <div class="icon">
        <div class="icon-circle">
          <span style="font-size:28px;">🔐</span>
        </div>
      </div>

      <h2>Password Reset Request</h2>

      <p>Hello,</p>

      <p>We received a request to reset your MySystem password.</p>

      <p>Click the button below to set a new password:</p>

      <p style="text-align:center;">
        <a href="{{resetLink}}" class="cta">Reset Password</a>
      </p>

      <p class="small">Or copy and paste this link into your browser:</p>
      <div class="link-box">
        <a href="{{resetLink}}">{{resetLink}}</a>
      </div>

      <div class="warning">
        <strong>Didn't request this?</strong><br>
        If you did not request this change, please ignore this email. Your password will remain unchanged.
      </div>
    </div>

    <div class="footer">
      <p>Best regards,<br><span class="logo">MySystem Security Team</span></p>
      <p>&copy; {{year}} MySystem | All rights reserved.</p>
    </div>
  </div>
</body>
</html>
